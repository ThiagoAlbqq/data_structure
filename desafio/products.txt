typedef struct ProductForQueue {
    int cod;
    products_enum type;
    char descricao[MAX_DESC];
    float preco;

    struct ProductForQueue *prox;
} ProductForQueue;

typedef struct {
    int cod;
    ProductForQueue *prox;
    int tamanho;
} ProductQueueOfLinkedList;

typedef struct {
    products_enum type;
    ProductQueueOfLinkedList *header;
    int tam;
} ProductBox;